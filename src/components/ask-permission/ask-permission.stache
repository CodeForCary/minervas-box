{{#is formState 'FORM'}}
    {{question}}
    <button class="btn btn-primary" type="button" ($click)="updateState('GRANTED')">Yep!</button>
    <button class="btn btn-default" type="button" ($click)="updateState('DENIED')">No, thanks.</button>
{{else}}
    <content />
{{/is}}
